apiVersion: v1
kind: Service
metadata:
  name: REPLACE_RELEASE_NAME-REPLACE_SERVICE_NAME
  namespace: auto-service-system
spec:
  selector:
    app: REPLACE_SERVICE_NAME
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
