<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <groupId>com.gearup</groupId>
    <artifactId>gearup-root</artifactId>
    <version>1.0.0</version>
    <packaging>pom</packaging>

    <name>GearUp Backend</name>

    <modules>
        <module>api-gateway</module>
        <module>config-server</module>
        <module>service-discovery</module>
        <module>services/automobile-service</module>
        <module>services/notification-service</module>

        <module>shared-libs/common-dto</module>
        <module>shared-libs/common-utils</module>
        <module>shared-libs/event-models</module>
        <module>shared-libs/security-lib</module>
    </modules>

    <properties>
        <!-- Spring Boot BOM to manage consistent versions (compatible with jakarta APIs) -->
        <spring.boot.version>3.1.6</spring.boot.version>
        <!-- Java target version for compilation -->
        <java.version>21</java.version>
        <!-- Firebase Admin SDK (modules can reference ${firebase.admin.version}) -->
        <firebase.admin.version>9.2.0</firebase.admin.version>

        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <maven.compiler.source>${java.version}</maven.compiler.source>
        <maven.compiler.target>${java.version}</maven.compiler.target>
        <maven.compiler.release>${java.version}</maven.compiler.release>
    </properties>

    <dependencyManagement>
        <dependencies>
            <!-- Import Spring Boot dependency management BOM -->
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-dependencies</artifactId>
                <version>${spring.boot.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>3.11.0</version>
                    <configuration>
                        <release>${maven.compiler.release}</release>
                    </configuration>
                </plugin>

                <plugin>
                    <groupId>org.springframework.boot</groupId>
                    <artifactId>spring-boot-maven-plugin</artifactId>
                    <version>${spring.boot.version}</version>
                </plugin>

                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-surefire-plugin</artifactId>
                    <version>3.1.2</version>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>

</project>
