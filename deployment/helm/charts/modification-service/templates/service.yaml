apiVersion: v1
kind: Service
metadata:
  name: modification-service
spec:
  selector:
    app: modification-service
  ports:
    - protocol: TCP
      port: { { .Values.service.port } }
      targetPort: { { .Values.service.port } }
  type: { { .Values.service.type } }
