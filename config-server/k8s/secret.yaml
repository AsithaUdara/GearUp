apiVersion: v1
kind: Secret
metadata:
  name: config-server-secret
type: Opaque
stringData:
  # Example credentials (replace in production);
  # store these in your cluster secrets management or external secret manager.
  username: configadmin
  password: change-me
