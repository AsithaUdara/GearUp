package com;

/*
 * Deprecated placeholder retained only because of prior structure.
 * This class is intentionally NOT a Spring Boot main application anymore.
 * The real entrypoint is com.gearup.automobileservice.DemoApplication.
 */
public final class DemoApplication {
    private DemoApplication() {}
    // No main method to avoid boot plugin picking this as a candidate.
}
