# API Endpoints Reference

This document lists HTTP endpoints found in the repository, grouped by service. Paths, HTTP methods, handler classes and short descriptions are included where available. It was generated by scanning controller classes in the codebase.

---

**User-Auth Service** (`services/user-auth-service`)

Base: `/api/v1/users` (UserController)

- POST `/api/v1/users/register` : Register a new user. (UserController.registerUser)
- GET `/api/v1/users/me` : Get current user profile. (UserController.getCurrentUser)
- GET `/api/v1/users/profile` : Alias for `/me`. (UserController.getUserProfile)
- PUT `/api/v1/users/me` : Update current user profile. (UserController.updateCurrentUser)
- POST `/api/v1/users/{userId}/roles` : Assign role to a user (ADMIN). (UserController.assignRole)
- GET `/api/v1/users/{userId}` : Get user by id (ADMIN). (UserController.getUserById)

Base: `/api/v1/auth` (AuthController)

- POST `/api/v1/auth/login` : Authenticate with Firebase ID token. (AuthController.login)
- POST `/api/v1/auth/refresh` : Refresh access token. (AuthController.refreshToken)
- POST `/api/v1/auth/logout` : Logout (invalidate token). (AuthController.logout)
- POST `/api/v1/auth/logout-all` : Logout from all sessions. (AuthController.logoutAll)
- POST `/api/v1/auth/setup-password` : Setup password (OTP flow). (AuthController.setupPassword)
- POST `/api/v1/auth/verify-otp` : Verify OTP. (AuthController.verifyOtp)

Base: `/api/v1/roles` (RoleController)

- GET `/api/v1/roles` : Get all roles. (RoleController.getAllRoles)
- GET `/api/v1/roles/{name}` : Get role by name. (RoleController.getRoleByName)

Base: `/api/v1/dev` (DevAdminController) [active in `dev` profile]

- POST `/api/v1/dev/admin/users/employees` : Create employee (dev-only helper). (DevAdminController.devCreateEmployee)

Base: `/api/v1/admin/users` (AdminUserController, requires ADMIN)

- PATCH `/api/v1/admin/users/{userId}/name` : Update user name. (AdminUserController.updateUserName)
- GET `/api/v1/admin/users` : List users (pagination + filters). (AdminUserController.getAllUsers)
- GET `/api/v1/admin/users/{userId}` : Get user details. (AdminUserController.getUserById)
- POST `/api/v1/admin/users/employees` : Create employee (admin). (AdminUserController.createEmployee)
- PUT `/api/v1/admin/users/{userId}` : Update user (role/status). (AdminUserController.updateUser)
- PATCH `/api/v1/admin/users/{userId}/deactivate` : Deactivate user. (AdminUserController.deactivateUser)
- PATCH `/api/v1/admin/users/{userId}/activate` : Activate user. (AdminUserController.activateUser)
- DELETE `/api/v1/admin/users/{userId}` : Delete (soft) user. (AdminUserController.deleteUser)
- GET `/api/v1/admin/users/stats` : Get user statistics. (AdminUserController.getUserStats)

**Vehicle Service** (`services/vehicle-service`)
Base: `/api/v1/vehicles` (VehicleController)

- GET `/api/v1/vehicles/user/{userId}` : Get vehicles by user. (VehicleController.getByUser)
- GET `/api/v1/vehicles/{vehicleId}` : Get vehicle by ID. (VehicleController.get)
- POST `/api/v1/vehicles` : Create vehicle. (VehicleController.create)
- PUT `/api/v1/vehicles/{vehicleId}` : Update vehicle. (VehicleController.update)
- DELETE `/api/v1/vehicles/{vehicleId}` : Delete vehicle. (VehicleController.delete)
- PATCH `/api/v1/vehicles/{vehicleId}/status` : Change vehicle status (query param `status`). (VehicleController.changeStatus)

**Parts Service** (`services/parts-service`)
Base: `/api/v1/parts-requests` (PartsRequestController)

- POST `/api/v1/parts-requests` : Create parts request. (PartsRequestController.createRequest)
- GET `/api/v1/parts-requests/my-requests` : Get user's requests (paged). (PartsRequestController.getUserRequests)
- GET `/api/v1/parts-requests/by-status/{status}` : Get requests by status (paged). (PartsRequestController.getRequestsByStatus)
- PUT `/api/v1/parts-requests/{requestId}/status` : Update request status (query `status`). (PartsRequestController.updateRequestStatus)

**Notification Service** (`services/notification-service`)
Base: `/api/v1/test` (TestNotificationController)

- POST `/api/v1/test/invoice-created` : Publish test InvoiceCreatedEvent. (TestNotificationController.testInvoiceCreated)
- POST `/api/v1/test/task-assigned` : Publish test TaskAssignedEvent. (TestNotificationController.testTaskAssigned)
- POST `/api/v1/test/invoice-paid` : Publish test InvoicePaidEvent. (TestNotificationController.testInvoicePaid)

Base: `/api/v1/notifications` (NotificationController)

- POST `/api/v1/notifications` : Create a notification. (NotificationController.createNotification)
- GET `/api/v1/notifications` : Get paginated notifications for authenticated user. (NotificationController.getUserNotifications)
- GET `/api/v1/notifications/unread` : Get unread notifications. (NotificationController.getUnreadNotifications)
- GET `/api/v1/notifications/unread/count` : Get unread count. (NotificationController.getUnreadCount)
- PATCH `/api/v1/notifications/{notificationId}/read` : Mark single notification as read. (NotificationController.markAsRead)
- PATCH `/api/v1/notifications/read-all` : Mark all notifications as read. (NotificationController.markAllAsRead)
- DELETE `/api/v1/notifications/{notificationId}` : Delete notification. (NotificationController.deleteNotification)

**Template Service** (`services/template-service`)
Base: `/api/v1/admin/service-templates` (AdminServiceTemplateController) [ADMIN]

- GET `/api/v1/admin/service-templates` : List templates (optional `activeOnly`). (AdminServiceTemplateController.list)
- GET `/api/v1/admin/service-templates/{id}` : Get template by id. (AdminServiceTemplateController.get)
- POST `/api/v1/admin/service-templates` : Create template. (AdminServiceTemplateController.create)
- PUT `/api/v1/admin/service-templates/{id}` : Update template. (AdminServiceTemplateController.update)
- DELETE `/api/v1/admin/service-templates/{id}` : Delete template. (AdminServiceTemplateController.delete)

**Payment Service** (`services/payment-service`)
Public reviews (PublicReviewController)

- GET `/api/v1/reviews/public` : Get published reviews (landing page). (PublicReviewController.getPublishedReviews)

Customer reviews (CustomerReviewController)

- POST `/api/v1/reviews/customer` : Submit a review (customer). (CustomerReviewController.submitReview)
- GET `/api/v1/reviews/customer` : Get customer's own reviews (`email` param). (CustomerReviewController.getMyReviews)

Admin reviews (AdminReviewController)

- GET `/api/v1/reviews/admin` : Get all reviews. (AdminReviewController.getAllReviews)
- GET `/api/v1/reviews/admin/status/{status}` : Get reviews by status. (AdminReviewController.getReviewsByStatus)
- GET `/api/v1/reviews/admin/pending` : Get pending reviews. (AdminReviewController.getPendingReviews)
- PUT `/api/v1/reviews/admin/{reviewId}/publish` : Publish review. (AdminReviewController.publishReview)
- DELETE `/api/v1/reviews/admin/{reviewId}` : Delete review. (AdminReviewController.deleteReview)
- GET `/api/v1/reviews/admin/stats` : Get review statistics. (AdminReviewController.getReviewStats)
- GET `/api/v1/reviews/admin/{reviewId}` : Get single review by ID. (AdminReviewController.getReviewById)

Customer payments (CustomerPaymentController)

- GET `/api/v1/payments/customer/bills` : Get customer bills (query `email` or `status`). (CustomerPaymentController.getCustomerBills)
- GET `/api/v1/payments/customer/bills/{id}` : Get bill by id. (CustomerPaymentController.getBillById)
- PUT `/api/v1/payments/customer/bills/{id}/mark-paid` : Mark bill as paid. (CustomerPaymentController.markBillAsPaid)

Admin payments (AdminPaymentController)

- POST `/api/v1/payments/admin/requests` : Create payment request. (AdminPaymentController.createPaymentRequest)
- GET `/api/v1/payments/admin/requests` : Get payment requests (optional `status`). (AdminPaymentController.getAllPaymentRequests)
- GET `/api/v1/payments/admin/requests/{id}` : Get specific payment request. (AdminPaymentController.getPaymentRequestById)
- PUT `/api/v1/payments/admin/requests/{id}/approve` : Approve payment request. (AdminPaymentController.approvePaymentRequest)
- PUT `/api/v1/payments/admin/requests/{id}/reject` : Reject payment request (body with `reason`). (AdminPaymentController.rejectPaymentRequest)
- GET `/api/v1/payments/admin/stats` : Get payment stats. (AdminPaymentController.getPaymentStats)

**Tracking Service** (`services/tracking-service`)
Base: `/api/v1/tracking` (TrackingController)

- GET `/api/v1/tracking/employee/{employeeId}/tasks` : Get all tasks for an employee. (TrackingController.getEmployeeTasks)
- GET `/api/v1/tracking/employee/{employeeId}/daily-summary` : Get daily summary. (TrackingController.getDailySummary)
- GET `/api/v1/tracking/employee/{employeeId}/modification-requests/pending` : Get pending modification requests for employee. (TrackingController.getPendingRequests)
- POST `/api/v1/tracking/modification-requests/{requestId}/create-task` : Create task from modification request (`employeeId` param). (TrackingController.createTaskFromRequest)
- POST `/api/v1/tracking/modification-requests` : Create modification request. (TrackingController.createModificationRequest)
- GET `/api/v1/tracking/vehicle/{vehicle}/modification-requests` : Get modification requests by vehicle. (TrackingController.getModificationRequestsByVehicle)
- POST `/api/v1/tracking/parts-requests` : Create parts request. (TrackingController.createPartsRequest)
- GET `/api/v1/tracking/vehicle/{vehicle}/parts-requests` : Get parts requests by vehicle. (TrackingController.getPartsRequestsByVehicle)
- GET `/api/v1/tracking/service/{serviceId}/parts-requests` : Get parts requests by service id. (TrackingController.getPartsRequestsByService)

Time logs (TimeLogController) — all under `/api/v1/tracking` as well

- POST `/api/v1/tracking/tasks/{taskId}/time-logs/start` : Start time log (`employeeId` param). (TimeLogController.startTimeLog)
- POST `/api/v1/tracking/tasks/{taskId}/time-logs/stop` : Stop time log. (TimeLogController.stopTimeLog)
- GET `/api/v1/tracking/tasks/{taskId}/time-logs` : Get all time logs for a task. (TimeLogController.getTimeLogsByTask)
- GET `/api/v1/tracking/tasks/{taskId}/time-logs/active` : Get active time log for task+employee. (TimeLogController.getActiveTimeLog)
- GET `/api/v1/tracking/employees/{employeeId}/time-logs` : Get all time logs for an employee. (TimeLogController.getTimeLogsByEmployee)

Tasks (TaskController) — base `/api/v1/tracking/tasks`

- POST `/api/v1/tracking/tasks` : Create new task. (TaskController.createTask)
- PUT `/api/v1/tracking/tasks/{taskId}` : Update task progress. (TaskController.updateTask)
- GET `/api/v1/tracking/tasks/{taskId}` : Get task by id. (TaskController.getTask)
- GET `/api/v1/tracking/tasks/vehicle/{vehicle}` : Get tasks by vehicle. (TaskController.getTasksByVehicle)
- GET `/api/v1/tracking/tasks/service/{serviceId}` : Get tasks by service id. (TaskController.getTasksByServiceId)

Progress (ProgressController) — base `/api/v1/tracking/progress`

- GET `/api/v1/tracking/progress/service/{serviceId}` : Get service progress. (ProgressController.getServiceProgress)
- PUT `/api/v1/tracking/progress/service/{serviceId}` : Update service progress (`currentStep` param). (ProgressController.updateProgress)

**Modification Service** (`services/modification-service`)
Service modifications (ServiceModificationController)

- GET `/api/v1/service-modifications` : List all services. (ServiceModificationController.getAllServices)
- GET `/api/v1/service-modifications/{serviceId}` : Get service and modification requests. (ServiceModificationController.getServiceWithModifications)
- GET `/api/v1/service-modifications/{serviceId}/refresh` : Refresh latest service info. (ServiceModificationController.refreshServiceInfo)

Modification requests (ModificationRequestController)

- POST `/api/v1/service-modifications/{serviceId}/requests` : Submit modification request (create). (ModificationRequestController.submitModificationRequest)
- GET `/api/v1/service-modifications/{serviceId}/requests` : List requests for service. (ModificationRequestController.getAllRequestsForService)
- GET `/api/v1/modification-requests/{requestId}` : Get single request. (ModificationRequestController.getSingleRequest)
- PATCH `/api/v1/modification-requests/{requestId}` : Update modification request. (ModificationRequestController.updateModificationRequest)
- DELETE `/api/v1/modification-requests/{requestId}` : Delete modification request. (ModificationRequestController.deleteModificationRequest)
- GET `/api/v1/customers/{customerId}/services` : Get services per customer. (ModificationRequestController.getServicesPerCustomer)

Simple services (SimpleServiceController)

- GET `/api/v1/services` : List services. (SimpleServiceController.getAllServices)
- GET `/api/v1/services/{id}` : Get service by id. (SimpleServiceController.getServiceById)

**Chatbot Service** (`services/chatbot-service`)
Base: `/api/v1` (TestController)

- GET `/api/v1/hello` : Simple health/test endpoint. (TestController.hello)

WebSocket (ChatWebSocketController)

- STOMP `/app/chat.send` : WebSocket message mapping for sending chat messages. (ChatWebSocketController.sendMessage)
- STOMP `/app/chat.typing` : Typing indicator. (ChatWebSocketController.handleTyping)

REST chat (ChatController) — base `/api/v1/chat`

- POST `/api/v1/chat/send` : Send message (REST). (ChatController.sendMessage)
- GET `/api/v1/chat/history/{sessionId}` : Get conversation history. (ChatController.getHistory)
- GET `/api/v1/chat/history/{sessionId}/recent` : Get recent messages (`limit` param). (ChatController.getRecentHistory)
- GET `/api/v1/chat/sessions` : Get sessions for a user. (ChatController.getUserSessions)
- POST `/api/v1/chat/sessions/{sessionId}/close` : Close session. (ChatController.closeSession)
- GET `/api/v1/chat/health` : Health check. (ChatController.health)

**Customer Service** (`services/customer-service`)
Base: `/api/v1/customers` (CustomerController)

- GET `/api/v1/customers/{firebaseUid}` : Get customer by firebaseUid. (CustomerController.get)
- POST `/api/v1/customers` : Create customer. (CustomerController.create)
- PUT `/api/v1/customers/{firebaseUid}` : Update customer. (CustomerController.update)
- PATCH `/api/v1/customers/{firebaseUid}/kyc` : Update KYC status (`status` param). (CustomerController.updateKyc)

**Analytical Service** (`services/analytical-service`)
Base: `/api/v1/analytics` (AnalyticsController)

- GET `/api/v1/analytics/dashboard` : Get dashboard data. (AnalyticsController.getDashboard)
- GET `/api/v1/analytics/metrics` : Get dashboard metrics. (AnalyticsController.getMetrics)
- GET `/api/v1/analytics/appointments/trend` : Get appointment trend (`weeks` param). (AnalyticsController.getAppointmentTrend)
- GET `/api/v1/analytics/services/top` : Get top services (`limit` param). (AnalyticsController.getTopServices)
- GET `/api/v1/analytics/activities/recent` : Get recent activities (`limit` param). (AnalyticsController.getRecentActivities)
- GET `/api/v1/analytics/health` : Health check. (AnalyticsController.health)
- POST `/api/v1/analytics/services` : Add service analytics (mock). (AnalyticsController.addServiceAnalytics)
- POST `/api/v1/analytics/services/popular` : Add popular service (mock). (AnalyticsController.addPopularService)
- POST `/api/v1/analytics/activities` : Add recent activity (mock). (AnalyticsController.addActivity)
- POST `/api/v1/analytics/customers` : Add customer analytics (mock). (AnalyticsController.addCustomerAnalytics)
- DELETE `/api/v1/analytics/services/{id}` : Delete service analytics. (AnalyticsController.deleteServiceAnalytics)
- DELETE `/api/v1/analytics/services/popular/{id}` : Delete popular service. (AnalyticsController.deletePopularService)
- DELETE `/api/v1/analytics/activities/{id}` : Delete activity. (AnalyticsController.deleteActivity)
- POST `/api/v1/analytics/reset` : Reset all analytics data. (AnalyticsController.resetAllData)

**Appointment Service** (`services/appointment-service`)
Bookings (BookingController) — base `/api/v1/bookings`

- POST `/api/v1/bookings` : Create booking. (BookingController.createBooking)
- GET `/api/v1/bookings` : Get all bookings. (BookingController.getAllBookings)
- GET `/api/v1/bookings/assigned` : Get bookings with assigned employees. (BookingController.getAssignedBookings)
- GET `/api/v1/bookings/{id}` : Get booking by id. (BookingController.getBookingById)
- PUT `/api/v1/bookings/{id}` : Update booking. (BookingController.updateBooking)
- PUT `/api/v1/bookings/{id}/approve` : Approve booking. (BookingController.approveBooking)
- PUT `/api/v1/bookings/{id}/assign` : Assign employee to booking (`employeeId` param). (BookingController.assignEmployee)
- DELETE `/api/v1/bookings/{id}` : Cancel booking. (BookingController.cancelBooking)

Time slots (TimeSlotController & CustomerTimeSlotController)

- GET `/api/v1/timeslots` : Get available time slots (`serviceId`, `date`). (TimeSlotController.getAvailableTimeSlots)
- GET `/api/v1/timeslots/{id}` : Get time slot by id. (TimeSlotController.getTimeSlotById)
- POST `/api/v1/timeslots` : Create time slot. (TimeSlotController.createTimeSlot)
- PATCH `/api/v1/timeslots/{id}` : Update time slot availability. (TimeSlotController.updateTimeSlotAvailability)
- DELETE `/api/v1/timeslots/{id}` : Delete time slot. (TimeSlotController.deleteTimeSlot)
- POST `/api/v1/timeslots/block-range` : Block date range (serviceId, startDate, endDate). (TimeSlotController.blockDateRange)
- POST `/api/v1/timeslots/unblock-range` : Unblock date range. (TimeSlotController.unblockDateRange)
- GET `/api/v1/timeslots/customer` : Customer-facing available slots (`serviceId`, `date`). (CustomerTimeSlotController.getAvailableTimeSlotsForCustomer)

Services & employees

- GET `/api/v1/services` : Get appointment services list. (ServiceController.getAllServices)
- GET `/api/v1/services/{id}` : Get service by id. (ServiceController.getServiceById)
- GET `/api/v1/employees/available` : Get available employees. (EmployeeController.getAvailableEmployees)

---

Notes & next steps

- This file was generated by scanning controller source files for mapping annotations. It includes HTTP methods, paths, handler class/method names and brief descriptions.
- I may have missed endpoints implemented via functional routing or added in other languages/configs (Kotlin, YAML, or gateway routing). If you want, I can also:
  - Scan for routes defined in `api-gateway` or `config-repo` (gateway proxy mappings / YAML routes).
  - Produce an OpenAPI-style JSON/YAML file or CSV for import into tools.

If you'd like, I can commit this file and open a PR or continue by scanning gateway configs. Which next step do you want?

**API Gateway / Proxy Routes**

The repository contains gateway route configurations in two places:

- `config-repo/api-gateway.yml` (central config delivered by config-server)
- `api-gateway/src/main/resources/application.yml` (gateway local config)

Below are the route path patterns and the destination services (load-balanced `lb://` names) discovered in those YAML files. Some routes overlap; both sources are shown when they differ.

- user-auth-service

  - Paths (config-repo): `/api/v1/auth/**`, `/api/v1/users/**`, `/api/v1/roles/**`, `/api/v1/admin/users/**` -> `lb://user-auth-service`
  - Paths (api-gateway app): `/api/v1/auth/**`, `/api/v1/users/**`, `/api/v1/admin/users/**` -> `lb://user-auth-service`

- chatbot-service

  - Paths (config-repo): `/api/v1/chat/**`, `/api/v1/hello` -> `lb://chatbot-service`
  - Paths (api-gateway app): `/api/chat/**`, `/api/v1/hello` -> `lb://chatbot-service` (note `/api/chat/**` vs `/api/v1/chat/**`)

- vehicle-service

  - Paths (config-repo): `/api/v1/vehicles/**` -> `lb://vehicle-service`
  - Paths (api-gateway app): `/api/v1/vehicles/**` -> `lb://vehicle-service`

- customer-service

  - Paths (config-repo): `/api/v1/customers/**` -> `lb://customer-service`
  - Paths (api-gateway app): `/api/v1/customers/**` -> `lb://customer-service`

- appointment-service

  - Paths (config-repo): `/api/v1/services/**`, `/api/v1/employees/**`, `/api/v1/timeslots/**`, `/api/v1/bookings/**` -> `lb://appointment-service`
  - Paths (api-gateway app): `/api/appointments/**`, `/api/bookings/**`, `/api/timeslots/**` -> `lb://appointment-service` (note `/api/...` vs `/api/v1/...`)

- payment-service

  - Paths (config-repo): `/api/v1/payments/**`, `/api/v1/reviews/**` -> `lb://payment-service`
  - Paths (api-gateway app): `/api/payments/**`, `/api/bills/**`, `/api/payment-requests/**` -> `lb://payment-service`

- tracking-service

  - Paths (config-repo): `/api/v1/tracking/**` -> `lb://tracking-service`
  - Paths (api-gateway app): `/api/tracking/**` -> `lb://tracking-service` (small variation on prefix)

- modification-service

  - Paths (config-repo): `/api/v1/service-modifications/**`, `/api/v1/modification-requests/**`, `/api/v1/customers/**` -> `lb://modification-service`
  - Paths (api-gateway app): `/api/modifications/**`, `/api/modification-requests/**` -> `lb://modification-service`

- notification-service

  - Paths (config-repo): `/api/v1/notifications/**`, `/api/v1/test/**` -> `lb://notification-service`
  - Paths (api-gateway app): `/api/v1/notifications/**`, `/api/v1/test/**` -> `lb://notification-service`

- analytical-service

  - Paths (config-repo): `/api/v1/analytics/**` -> `lb://analytical-service`
  - Paths (api-gateway app): `/api/v1/analytics/**` -> `lb://analytical-service`

- parts-service

  - Paths (config-repo): `/api/v1/parts-requests/**` -> `lb://parts-service`
  - Paths (api-gateway app): `/api/parts/**`, `/api/parts-requests/**` -> `lb://parts-service`

- template-service

  - Paths (config-repo): `/api/v1/admin/service-templates/**` -> `lb://template-service`
  - Paths (api-gateway app): `/api/v1/admin/service-templates/**` -> `lb://template-service`

- api-gateway root/default
  - Path (api-gateway app): `/api/**` -> `lb://api-gateway` (StripPrefix parts:1)

Notes:

- `config-repo/api-gateway.yml` appears to be the central canonical routing configuration. The `api-gateway` service local `application.yml` contains similar routes (some path-prefix variations like `/api/...` vs `/api/v1/...`).
- Gateway filters applied: most routes use `StripPrefix=0` or keep full path (some routes use request rate limiter filters). Global CORS is configured in both places.

Suggested next steps (optional):

- Normalize and canonicalize route prefixes (decide between `/api/` and `/api/v1/`) and update docs accordingly.
- Generate an aggregated CSV or OpenAPI gateway proxy summary for external tooling.
