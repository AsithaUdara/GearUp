apiVersion: v1
kind: Service
metadata:
  name: tracking-service
  namespace: default
  labels:
    app: tracking-service
    tier: backend
spec:
  type: ClusterIP
  selector:
    app: tracking-service
  ports:
    - name: http
      protocol: TCP
      port: 8086
      targetPort: 8086
